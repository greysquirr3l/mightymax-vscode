.git/
.github/
.vscode/
.claude/
.coraline/
.npm-cache/
.npm-tmp/
src/
test/
node_modules/
out/
**/*.map
**/*.ts
**/*.test.ts
.vscode-test.mjs
.tmp-test/
features.json
mighty-max-plan.toml
tsconfig.json
eslint.config.mjs
esbuild.config.mjs
.prettierrc.json
.mmarkdownlint.json
.markdownlintignore
.gitignore
.markdownlint.json
error_from_console.txt
*.md
!README.md
!CHANGELOG.md
!LICENSE.md

# Internal-only paths that the `*.md` glob (which matches basenames
# only) leaves through. Each line below excludes every file under the
# prefix regardless of basename.
tasks/
docs/dev/

# Internal CI / scanner configs not meant to ship to the
# Marketplace.
.gitleaks.toml

# Stray / untracked logo images not referenced by package.json
# `icon`, the README, or the CHANGELOG. mighty_max_logo_readme.png
# is a candidate for cleanup once the README is updated; the head2
# files are local variants that should never ship.
assets/img/mightymax_head2.png
assets/img/mightymax_head2_inv.png
assets/img/mighty_max_logo_readme.png
